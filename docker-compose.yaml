services:
  auth:
    build:
      context: .
      dockerfile: ./apps/auth/Dockerfile
      target: development
    command: pnpm run start:dev auth
    env_file:
      - ./apps/auth/.env
    ports:
      - '3001:3001'
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
  properties:
    build:
      context: .
      dockerfile: ./apps/properties/Dockerfile
      target: development
    command: pnpm run start:dev properties
    env_file:
      - ./apps/properties/.env
    ports:
      - '3006:3006'
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
  database:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    env_file:
      - .env
