apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sleepr
spec:
  rules:
    - http:
        paths:
          - path: /auth/*
            pathType: ImplementationSpecific
            backend:
              service:
                name: auth-http
                port:
                  number: 3003
          - path: /properties/*
            pathType: ImplementationSpecific
            backend:
              service:
                name: properties-http
                port:
                  number: 3006